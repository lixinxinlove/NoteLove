<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.lixinxinlove.notelove">

    <dist:module dist:instant="true"/>

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name=" android.permission.VIBRATE"/>

    <application
            android:name=".app.NoteApp"
            android:allowBackup="true"
            android:icon="@mipmap/logo"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.TestActivity">
        </activity>
        <activity android:name=".activity.TuJiaHomeActivity">
        </activity>
        <activity android:name=".activity.StackLayoutManagerActivity">
        </activity>

        <meta-data
                android:name="android.max_aspect"
                android:value="2.1"/>

        <activity android:name=".activity.BaiduAiActivity">
        </activity> <!-- 选择时间 -->
        <activity android:name=".activity.SelectTimeActivity"/>
        <activity android:name=".activity.MyselfActivity"/> <!-- 图片裁剪 -->
        <activity
                android:name="com.yalantis.ucrop.UCropActivity"
                android:screenOrientation="portrait"
                android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <service
                android:name=".server.NoteSyncService"
                android:enabled="true"
                android:exported="true">
        </service> <!-- 登录 -->
        <activity android:name=".activity.LoginActivity"/> <!-- 编辑 -->
        <activity android:name=".activity.EditActivity"/> <!-- 欢迎页 -->
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity> <!-- 列表页 -->
        <activity
                android:name=".activity.NoteListActivity"
                android:label="@string/app_name"/>
    </application>

</manifest>